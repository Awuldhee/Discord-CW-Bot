# Private Google credentials

This folder is intentionally excluded from Git.

For Google Drive backup, place the following files here on the machine/server running the bot:

- `client_secret.json` — OAuth 2.0 Desktop Client credentials downloaded from Google Cloud.
- `token.json` — OAuth token generated by `node backup/authorize.js`.

Never commit these files to a public repository.
